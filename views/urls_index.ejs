<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp</title>
</head>

<body style="font-size:20px; padding:50px;">
  <%- include partials/_header %>
  <h1> ShortURL - URL </h1>
    <% for (var i in urls) { %>
      <li><b><%= i %></b> - <a href=<%= urls[i].url %>><%= urls[i].url %></a></li>
        <% if (user_id) { %>
        <form method="GET" action="/urls/<%= i %>">
          <button>Edit</button>
        </form>
        <form method="POST" action="/urls/<%= i %>/delete">
          <button>Delete</button><br />
        </form>
        <% } %>
    <% } %>
    <form method="GET" action="/urls/new">
      <button>New</button>
    </form>
</body>
</html>