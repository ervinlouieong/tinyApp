<!DOCTYPE html>

<html lang="en">
<head>
  <title>TinyApp</title>
</head>

<body>
    <%- include partials/_header %>
    <h1> ShortURL - URL </h1>
      <% for (var i in urls) { %>
        <li><b><%= i %></b> - <a href=<%= urls[i] %>><%= urls[i] %></a></li>
          <form method="GET" action="/urls/<%= i %>">
            <button>Edit</button>
          </form>
          <form method="POST" action="/urls/<%= i %>/delete">
            <button>Delete</button>
          </form>
      <% } %>
</body>
</html>